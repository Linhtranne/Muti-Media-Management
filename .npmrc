# .npmrc — force npm scripts to run via cmd.exe on Windows
# This avoids the PowerShell NativeCommandError false-failure
# when npm scripts have stderr output (e.g. ExperimentalWarning).
script-shell=cmd
